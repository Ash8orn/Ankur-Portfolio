# The lint toolchain (ESLint 10 / plugins) and TypeScript 7.0 currently ship
# conservative peer-dependency ranges. The combination builds, lints, and
# type-checks cleanly, so relax peer resolution to keep installs green on
# Vercel, CI, and local machines.
legacy-peer-deps=true
